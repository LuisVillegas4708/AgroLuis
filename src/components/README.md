# /src/components

Componentes UI reutilizables — NO específicos de un módulo.

- `/layout` — esqueleto visual (Sidebar, Header, AppLayout).
- `/common` — badges, banners, botones especiales, spinners, etc.

Si un componente es exclusivo de un módulo, vive dentro de `/src/modules/<modulo>/`.
