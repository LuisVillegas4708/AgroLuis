# /src/lib

Utilidades de bajo nivel — clientes externos, helpers, cálculos.

- `supabase.js` — singleton del cliente Supabase. Configurado con anon key (RLS aplica).
- (próximamente) `triage.js`, `geo.js`, `calculadoras.js`, etc.
