# /src/store

Estado global con Zustand. **Solo memoria volátil de UI** —
sesión cacheada, parcela activa, estado del sidebar.

La verdad de los datos vive en Supabase. Esto es nada más para no
re-pedir la misma información en cada componente.
